# Card Field OCR

Browser-local screenshot OCR for checkout/billing fields. Open the GitHub Pages URL — no installer.

Paste a screenshot (Ctrl+V) or use **Read from clipboard** / choose image. Recognition runs in your browser via Tesseract.js.
